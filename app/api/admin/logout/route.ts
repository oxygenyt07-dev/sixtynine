import {NextResponse} from 'next/server';import {cookies} from 'next/headers';import {serverSupabase} from '@/lib/supabase';import {LOCAL_ADMIN_COOKIE} from '@/lib/auth';export async function POST(){(await cookies()).delete(LOCAL_ADMIN_COOKIE);const s=await serverSupabase();if(!s)return NextResponse.json({message:'Logged out.'});await s.auth.signOut();return NextResponse.json({message:'Logged out.'})}
