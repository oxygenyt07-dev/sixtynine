import {Download,FileArchive,FileText,Image as ImageIcon,Package} from 'lucide-react';import type {FileItem} from '@/types';
const icon=(m:string)=>m.includes('image')?ImageIcon:m.includes('text')?FileText:m.includes('zip')||m.includes('rar')?FileArchive:Package;function fmt(n:number){if(n<1024)return n+' B';if(n<1048576)return (n/1024).toFixed(1)+' KB';if(n<1073741824)return (n/1048576).toFixed(1)+' MB';return (n/1073741824).toFixed(2)+' GB'}
export default function FileCard({f}:{f:FileItem}){const I=icon(f.mime_type);const target=f.external_url||`/api/download/${f.id}`;return <article className="glass gaming-card group rounded-2xl p-5"><div className="flex items-start justify-between gap-4"><div className="grid h-11 w-11 place-items-center rounded-xl bg-indigo-400/10 text-indigo-300"><I/></div><span className="rounded-full border border-white/10 px-2 py-1 text-[10px] uppercase tracking-wider text-zinc-400">{f.category}</span></div><h3 className="mt-5 line-clamp-1 font-bold">{f.name}</h3><p className="mt-2 line-clamp-2 min-h-10 text-sm text-zinc-400">{f.description||'No description provided.'}</p><div className="mt-5 flex items-center justify-between text-xs text-zinc-500"><span>{f.size_bytes?fmt(f.size_bytes):'External link'}</span><span>{f.downloads} downloads</span></div><a href={target} target={f.external_url?'_blank':undefined} rel={f.external_url?'noopener noreferrer':undefined} className="mt-4 flex w-full items-center justify-center gap-2 rounded-xl bg-indigo-400 px-4 py-2.5 text-sm font-bold text-white hover:bg-indigo-300"><Download className="h-4 w-4"/>{f.external_url?'Open download':'Download'}</a></article>}
